# mimo-hyperparameter-optimization
A Python project demonstrating MIMO (Multiple-Input, Multiple-Output) neural networks with hyperparameter optimization using Keras Tuner to predict both continuous and binary targets simultaneously.
